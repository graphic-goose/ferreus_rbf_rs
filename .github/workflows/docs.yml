name: Deploy docs for ferreus_rbf and ferreus_bbfmm

on:
  push:
    branches: [ main, master ]
    paths:
      - "py_ferreus_rbf/**"
      - "py_ferreus_bbfmm/**"
      - ".github/workflows/docs.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "docs-deployment"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install documentation dependencies
        run: |
          python -m pip install --upgrade pip
          pip install mkdocs-material mkdocstrings[python] mkdocs-autorefs pymdown-extensions

      - name: Build ferreus_rbf docs
        working-directory: py_ferreus_rbf
        run: mkdocs build --strict --site-dir ../site/ferreus_rbf

      - name: Build ferreus_bbfmm docs
        working-directory: py_ferreus_bbfmm
        run: mkdocs build --strict --site-dir ../site/ferreus_bbfmm

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

