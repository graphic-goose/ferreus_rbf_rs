name: Build and Deploy Docs

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install Zensical dependencies
        run: |
          python -m pip install --upgrade pip
          pip install \
            zensical \
            mkdocstrings-python \
            pymdown-extensions

      - name: Build ferreus_rbf docs
        working-directory: py_ferreus_rbf
        run: zensical build -d ../site/ferreus_rbf

      - name: Build ferreus_bbfmm docs
        working-directory: py_ferreus_bbfmm
        run: zensical build -d ../site/ferreus_bbfmm

      - name: Build ferreus_rmt docs
        working-directory: py_ferreus_rmt
        run: zensical build -d ../site/ferreus_rmt

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
